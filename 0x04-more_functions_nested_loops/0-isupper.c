#include "holberton.h"
/**
 * _isupper - return 1 if c is uppercase
 * @c: params
 * Description: an integer
 * 
 * Return: 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
