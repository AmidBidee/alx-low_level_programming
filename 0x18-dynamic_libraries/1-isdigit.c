#include "main.h"
/**
 * _isdigit - check if c is a digit
 * @c: params
 * Description: an integer
 *
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
