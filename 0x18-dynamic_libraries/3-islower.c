#include "main.h"
/**
 * _islower - Entry point
 *
 * @c: for something
 * Description: Why do you need to know
 *
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
